osascript -e '
tell application "Google Chrome Canary" to set active tab index of first window to 1

tell application "Google Chrome"
    tell the active tab of its first window
        reload
    end tell
end tell
'

# tell application "Google Chrome"
#    reload every tab of window 1
# end tell


